const firstName = (firstName) => { firstName.toUpperCase()}

const lastName = (lastName) => {lastName.toLowerCase()}

//exports.firstName;
//exports.lastName;
 
exports ={
    firstName, lastName
};